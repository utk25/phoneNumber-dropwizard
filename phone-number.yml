# Database settings.
database:
    # the name of the JDBC driver, mysql in our case
    driverClass: com.mysql.jdbc.Driver
    # the username
    user: root
    # the password
    password: utkarsh25
    # the JDBC URL; the database is called phonenumber
    url: jdbc:mysql://localhost:3306/phonenumber